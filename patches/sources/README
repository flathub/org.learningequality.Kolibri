The scripts in this directory are management commands that need to be injected into the Kolibri source. If you need to change them, edit them here and then run build_patches.sh (or run the Makefile, which will run that as part of the build process.)